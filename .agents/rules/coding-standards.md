# Coding Standards

Follow these coding standards without exception:

1. **Remove Unused Styles**: Always clean up and remove any CSS or styles that are no longer being used.
2. **Linting**: Always run the linter and proactively fix all lint issues before considering a task complete.
3. **Strict Typing**: Never use `any` type or `unknown` types. All code must be properly and strictly typed.
